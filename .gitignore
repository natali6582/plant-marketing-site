# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# wrangler local state (temp builds, cache) — never committed
.wrangler/

# Working proposals — drafts, not site content. Kept out of the repo on purpose;
# ignored because a broad "git add" has swept one in twice.
docs/proposals/
